# Environment Variables for SmartRAG Demo
# Copy this file to .env and update with your values
# ALL environment variables are REQUIRED - no fallback values for security

# SQL Server Configuration (REQUIRED)
SQLSERVER_SA_PASSWORD=YourSecurePassword123!

# MySQL Configuration (REQUIRED)
MYSQL_ROOT_PASSWORD=YourMySQLPassword456!

# PostgreSQL Configuration (REQUIRED)
POSTGRES_PASSWORD=YourPostgresPassword789!

# Usage:
# 1. Copy this file: cp env.example .env
# 2. Update the passwords above with secure values
# 3. Run: docker-compose up -d
# 4. Never commit .env file to Git
# 5. All passwords are required - no default fallbacks for security
